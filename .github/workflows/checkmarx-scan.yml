name: Checkmarx One Scan
on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
permissions:
  contents: read

jobs:
  execute-checkmarx-scan:
    permissions:
      security-events: write
      contents: read
      pull-requests: write
      statuses: write
    runs-on: cx-public-ubuntu-x64
    steps:
      - name: Fetch Code
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
  
      - name: Scan with CxOne++ Action
        id: scan
        uses: checkmarx-ts/cxone-plusplus-github-action@9e69646151d1564d227e7979ce9780b422bdf2f4 # v2
        with:
          cx-tenant: ${{ secrets.CXONE_TENANT }}
          cx-client-id: ${{ secrets.CXONE_CLIENT_ID }}
          cx-client-secret: ${{ secrets.CXONE_CLIENT_SECRET }}
          cx-cli-debug: true
          build-container-tag: amazoncorretto:17
          upload-sarif-file: false
